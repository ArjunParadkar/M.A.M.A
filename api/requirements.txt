# NOTE: This repo is on Python 3.13; pin versions that have wheels/support.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.10.0
# Keep httpx compatible with supabase-py 2.3.4
httpx>=0.24,<0.26

# AI Model dependencies
numpy==1.26.2
# pandas and scikit-learn are intentionally omitted for Python 3.13 compatibility in this demo.
# If you later pin Python to 3.11/3.12, you can add them back for ML training/inference.

# F3 Vision Quality Check (CLIP) - Optional for now
# torch==2.1.1
# torchvision==0.16.1
# transformers==4.35.2
# pillow==10.1.0
# clip-by-openai==1.0

# Supabase client (for database/file access)
supabase==2.3.4

# STL processing
numpy-stl==3.1.0

# Image processing for F3 Quality Check
Pillow>=10.0.0

